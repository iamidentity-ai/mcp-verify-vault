```bash
git clone https://github.com/iamidentity-ai/mcp-verify-vault.git
cd mcp-verify-vault
```

Expected: a new directory `mcp-verify-vault/` with `infra/`, `mcp-server/`, `agent/`, `docs/`, and `scripts/` subdirectories.

The chapters that follow are the same prose you see in the per-chapter files under `docs/`. The two are kept in lockstep by `scripts/assemble-cookbook.sh`; edit the per-chapter file and re-run the script to refresh this assembled document.
